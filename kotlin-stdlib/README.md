# kotlin-stdlib

This is a stub project whose only purpose is to compile the currently used
Kotlin stdlib into a dex file, to be injected by Aliucord Manager.

This cannot be merged with Injector as it would override existing the
existing minified and obfuscated Kotlin classes in Discord v126.21
